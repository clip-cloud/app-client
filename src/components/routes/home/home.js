import React from "react";

import './home.css';

export default function Home() {
    // Render the home page with required input fields and submit button
    return (
        <div className="home_page">
            <span>This is the homepage!</span>
        </div>
    );
}